# Screenshots

Real-data captures of the four terminal pages, referenced from the root `README.md`.
Drop the following PNGs here (dark terminal theme, ~1440px wide):

- `dashboard.png` — watchlist ledger, TVL / unlocks / BTC on-chain band
- `research.png` — a MiCA answer with inline citation seals
- `briefs.png` — a daily brief detail view
- `anomalies.png` — the anomaly feed with z-score / depeg events

Until captured, the README image links render as broken placeholders on purpose;
nothing here claims a screenshot exists that does not.
